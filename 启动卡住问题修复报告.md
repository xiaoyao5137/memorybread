# 启动卡在初始化界面问题修复报告

## 问题描述

记忆面包 macOS 应用启动后一直卡在初始化界面，提示"启动时间较长，可能需要 5 分钟，请耐心等待…"，但实际等待超过 10 分钟也无法完成初始化。

## 根本原因分析

通过排查日志和测试，发现了两个关键问题：

### 1. 后端服务路径错误（已修复）

**问题：** 
`desktop-ui/src-tauri/src/lib.rs` 中的 `bundled_helper_path` 函数在查找 `memory-bread-ai` 时使用了错误的路径：

```rust
// 错误路径
.join("Helpers")
.join("memory-bread-ai.app")
```

**实际路径：**
```
Contents/Resources/binaries/memory-bread-ai.app
```

这是因为 `tauri.conf.json` 中 `memory-bread-ai.app` 被配置为 `resources` 而不是 `externalBin`，所以会被放在 `Resources/binaries/` 目录。

**修复：**
```rust
// 正确路径
.join("Resources")
.join("binaries")
.join("memory-bread-ai.app")
```

文件：`desktop-ui/src-tauri/src/lib.rs:1025`

### 2. PyInstaller 打包缺少 _struct 模块（主要问题）

**问题：**
PyInstaller 6.21.0 在 macOS 上打包 Python 3.9 应用时，无法正确加载 C 扩展模块 `_struct`，导致启动失败：

```
[PYI-25800:ERROR] Module object for struct is NULL!
Traceback (most recent call last):
  File "struct.py", line 13, in <module>
ModuleNotFoundError: No module named '_struct'
```

**诊断过程：**
1. 手动运行打包后的二进制文件：
   ```bash
   /Applications/记忆面包.app/Contents/Resources/binaries/memory-bread-ai.app/Contents/MacOS/memory-bread-ai model-api
   ```

2. 确认 `_struct.cpython-39-darwin.so` 文件存在于：
   ```
   Contents/Frameworks/python3__dot__9/lib-dynload/_struct.cpython-39-darwin.so
   ```

3. 但 PyInstaller 的 bootloader 无法正确加载该模块

**根本原因：**
PyInstaller 6.21.0 在 macOS 上与 Python 3.9 有兼容性问题，特别是在处理 C 扩展模块时。这是 PyInstaller 的已知 bug，在 6.22+ 版本中已修复。

**修复：**
升级 PyInstaller 到 6.22 或更高版本：

```diff
# ai-sidecar/requirements-build.txt
-pyinstaller==6.21.0
+pyinstaller>=6.22.0
```

## 验证步骤

### 1. 验证后端服务路径
```bash
./verify-backend-paths.sh
```

预期输出：
- ✓ memory-bread-core 存在于 `Contents/MacOS/`
- ✓ memory-bread-ai 存在于 `Contents/Resources/binaries/`
- ✓ 端口 7070 和 7071 正在监听

### 2. 测试手动启动后端服务
```bash
/Applications/记忆面包.app/Contents/Resources/binaries/memory-bread-ai.app/Contents/MacOS/memory-bread-ai model-api
```

应该能够正常启动，不再报告 `_struct` 模块错误。

### 3. 完整应用测试
1. 重新构建应用：
   ```bash
   ./scripts/build-macos.sh
   ```

2. 安装新版本的 DMG

3. 启动应用，验证：
   - 初始化流程能够正常完成（约 2-3 分钟）
   - 不再卡在初始化界面
   - 后端服务（7070, 7071）正常启动

## 相关文件

- `desktop-ui/src-tauri/src/lib.rs` - 后端服务路径查找逻辑
- `ai-sidecar/requirements-build.txt` - PyInstaller 版本
- `desktop-ui/src-tauri/tauri.conf.json` - Tauri 打包配置
- `scripts/build-macos.sh` - macOS 构建脚本

## 参考资料

- PyInstaller Issue: https://github.com/pyinstaller/pyinstaller/issues/7836
- macOS C 扩展模块打包问题: https://github.com/pyinstaller/pyinstaller/pull/7865

## 提交信息

```
fix: 修复 macOS 启动卡在初始化界面的问题

- 修正 memory-bread-ai 服务路径查找逻辑（Resources/binaries）
- 升级 PyInstaller 到 6.22+ 解决 _struct 模块加载问题

修复前：启动后端服务失败，导致初始化无法完成
修复后：后端服务正常启动，初始化流程顺利完成

影响范围：macOS DMG 打包
```
