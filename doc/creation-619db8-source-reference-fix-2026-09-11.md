# 最新创作的冲突复核来源绑定修复

## 故障与根因

本机历史记录 157，会话 `creation-1789028274294-d7af37f0619db8`，2026-09-11 00:13:37 保存为失败，正文 2333 字符已保留。原运行日志显示 `_resolve_source_audit_conflict` 在来源逐字绑定处抛出 `ValueError: 冲突复核来源未逐字绑定`，随后映射为 `CREATION_DELIVERY_UNVERIFIED / 无法核验冲突复核结果`。此次队列执行耗时 454653 ms。

上一轮已约束候选正文引用，但冲突复核仍要求模型分别生成来源 ID 和自由引文，再校验其逐字匹配、唯一性和单行边界。解码协议可以生成运行时拒绝的组合，来源错配、改写、跨行和重复文本均可能造成验收协议失败。原故障的原始模型响应未落盘，因此不能进一步断言当次属于哪一种字符串不匹配。

## 修改

- 来源证据改用 `source_ref` 枚举。代码按实际来源字段、行及最多 480 字符生成引用目录，保留来源 ID、字段位置和字符区间。
- 模型选择单一引用 ID，程序恢复其来源；不再生成来源 ID 与引文的自由组合。重复文本由原始位置区分，引用不跨字段或换行。
- 用户授权、未知来源、有依据却无引用、时间范围不支持、重复 JSON 键、无效正文引用和 uncertain 均继续拒绝。代码定位不证明语义蕴含，不把失败或不确定结论自动改为通过。
- 冲突复核解码顺序调整为候选引用、来源引用、理由、结论，避免在比对证据之前提前输出结论。定向真实模型首轮曾出现理由与结论相反，未计作语义通过；顺序调整后，同一实际位置返回来源支持，理由与引用一致。
- 公开验收报告、原会话正文和脑暴决策不变。

## 自动验证

689 项相关回归通过，覆盖交付契约、来源复核、审计原文绑定、Agent 执行、恢复边界及输入上下文。新增覆盖固定引用 schema、重复文本定位、长来源分片、字段与换行边界、未知引用、旧自由字段拒绝及日期范围拒绝。

两个修改的 Python 文件通过 Python 3.9 AST、注解联合类型语法检查和 Python 3.9.25 实际编译。没有打包 DMG。

## 本机证据

真实模型请求、响应、断点副本、测试日志与加载日志仅保留于 `/tmp/creation-619db8-source-ref/`，不把用户正文纳入版本化报告。

针对原日志中 `source_scope_grounding` 的同一正文位置，使用真实正文、来源和确认选择调用独立复核；其他检查为受控夹具，因此该定向结果不代表整稿验收。最终调用耗时 64.02 秒，返回 `source_supported`，无协议异常，理由与“开放事项、禁止编造”的实际原文一致。

最终从实际失败断点副本完成全流程验收重放：前六次使用本次真实模型录制响应，并断言请求参数逐项完全一致；后两次由最终代码重新调用真实模型。共八次逻辑调用，最终阶段耗时 80.31 秒，完成全部批次和选择覆盖检查，返回 `revise`（13 项检查、5 项待修订），没有来源绑定或验收协议异常。三项原冲突均返回来源支持，理由与引用一致。

只读 SQLite 对照确认原历史整行摘要未变。该结果证明故障链恢复，不代表原稿通过全部内容验收；没有替用户修改正文、选择答案或把失败历史改成完成。

最终受管服务 PID 81387，进程启动晚于最终 Python 源码更新，8001 端口 `/health` 返回 `ok`。
