# 最新创作执行失败修复验收

日期：2026-09-05

## 故障定位

本机历史 132 于 22:50:08 发起 direct/local 创作，42 毫秒后标记 failed，正文为空、执行轨迹为空。Core 日志确认接收请求，尚未进入模型执行。

本机已登记 `111_creation_operations`，但实际表仍是早期结构，缺少后续仓库代码依赖的 `original_document` 和 `retry_checkpoint_json`。只改已有 CREATE TABLE IF NOT EXISTS 迁移无法升级已安装数据库。只读 EXPLAIN 验证插入失败：`table creation_operations has no column named original_document`。原处理逻辑把数据库错误统一压成 409 CREATION_OPERATION_INVALID，页面最终显示通用失败提示。

## 修复

- 新增 112 迁移，按实际列存在情况在同一事务内补列并登记迁移。新增 original_document 时从 base_document 回填；已有原文与重试检查点保留。支持初版、部分升级及最新结构，重复启动不修改已有值。
- 区分指令冲突与数据库故障。写入失败返回 500 和可操作的中文提示，记录内部错误，响应不包含 SQL 或用户内容。
- 真实验收进一步发现：本地模型流以空白片段开头时，增量完整性检查立即以 empty_document 中断。调整为流中仅阻止实质性损坏，空正文检查保留在结束阶段。空白后跟正文能正常完成，全部为空白的流仍拒绝提交。

## 验证

- Core 数据库迁移回归：20 passed，覆盖缺少两列、仅缺一列、列已存在、数据保留和重复启动。
- Core API 回归：70 passed。
- Core 模型边界回归：1 passed。
- Core 创作操作集成回归：2 passed；其中恢复、重放、确认保存及撤销链路使用已登记旧 111 的数据库升级后执行，另验证数据库错误不会被误报为指令冲突或泄露内部详情。
- Python 创作执行回归：191 passed。
- Python 队列、云端协议、Markdown 与创作操作回归：83 passed。
- 修改的两个 Python 文件通过 Python 3.9 语法及类型联合检查；git diff --check 通过。
- 本机 Core 于 22:55:10 自动执行 112 成功，后续运行二进制于 22:58:58 构建、22:59:00 启动；Creation Service 于 23:01:23 加载晚于源码修改的进程。启动脚本健康检查通过。
- 真实本地文档验收：独立历史 134 首次命中空白流缺陷，修复后恢复同一操作，12.9 秒收到 run.completed，254 字符正文成功持久化，历史及操作均为 completed。

## 边界

历史 132 与原始需求保持不变，可在原会话重试。验收历史 133（直接回答）和 134（短文档）用于验证真实执行与落库，不代表原始长文内容质量验收。未打包 DMG，未提交工作区其他已有修改。
