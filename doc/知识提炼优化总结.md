# 知识提炼功能优化总结

## 修改日期
2026-03-09

## 问题与解决方案

### 1. 模型下载界面未显示 Qwen2.5-3B

**问题**：
- 本地已使用 `qwen2.5:3b` 模型
- 模型管理界面只显示 `qwen3.5:4b` 和 `qwen3.5:7b`
- 默认模型设置错误

**解决方案**：
- 修改 `ai-sidecar/model_manager.py`
- 在 `AVAILABLE_MODELS` 中添加 `qwen2.5-3b` 定义
- 设置为默认模型（`is_default=True`）

**修改文件**：
- `ai-sidecar/model_manager.py` (第 49-61 行)

---

### 2. 知识库推理内容过于精简

**问题**：
- 当前只生成 50-200 字的简短摘要
- 缺少详细内容，后期追溯困难
- 无法区分"概述"和"明细"

**解决方案**：
- 修改数据结构，增加 `overview` 和 `details` 字段
- `overview`：简洁描述正在做什么（30-100字）
- `details`：详细记录具体内容（200-500字）
- 保留 `summary` 字段用于向后兼容

**数据库迁移**：
- 创建迁移脚本 `004_add_overview_details.sql`
- 添加 `overview` 和 `details` 字段
- 更新 FTS 全文索引
- 重建触发器

**修改文件**：
- `ai-sidecar/knowledge/extractor_v2.py` - 更新 SYSTEM_PROMPT
- `ai-sidecar/knowledge/manager.py` - 更新 `add_entry` 方法
- `ai-sidecar/background_processor.py` - 更新数据库插入逻辑
- `ai-sidecar/migrations/004_add_overview_details.sql` - 数据库迁移

**LLM 配置调整**：
- 增加 `num_predict` 从 256 到 1024 tokens
- 支持生成更详细的内容

---

### 3. 相同事情应该合并到同一条知识

**问题**：
- 前后时刻采集到的相同工作内容被分散存储
- 只是简单计数，没有合并详细内容

**解决方案**：
- 修改知识去重逻辑
- 发现相似知识时，合并 `details` 内容而不是简单计数
- 追加新的细节信息，标注时间戳
- 使用语义相似度（余弦相似度 >= 0.85）判断是否为同一事情

**实现细节**：
```python
# 合并明细：保留原有内容，追加新内容
merged_details = existing_details
if details and details not in existing_details:
    merged_details += f"\n\n--- 补充 ({datetime.now().strftime('%Y-%m-%d %H:%M')}) ---\n{details}"
```

**修改文件**：
- `ai-sidecar/knowledge/extractor_v2.py` - 更新 `extract_sync` 方法

---

### 4. 向量计算错误修复

**问题**：
- `unsupported operand type(s) for *: 'EmbeddingVector' and 'EmbeddingVector'`
- 向量模型返回的是 `EmbeddingVector` 对象，不是 numpy 数组

**解决方案**：
- 修改 `_find_similar_knowledge` 方法
- 正确提取 `EmbeddingVector.vector` 属性
- 转换为 numpy 数组进行计算

**修改代码**：
```python
new_embedding = self.embedding_model.encode([overview])[0]
new_vector = np.array(new_embedding.vector)
```

**修改文件**：
- `ai-sidecar/knowledge/extractor_v2.py` - 更新 `_find_similar_knowledge` 方法

---

### 5. JSON 解析错误优化

**问题**：
- LLM 生成的 JSON 包含未转义的换行符
- 导致 JSON 解析失败

**解决方案**：
- 优化 SYSTEM_PROMPT，明确要求：
  - 不要在字符串中使用换行符
  - 使用空格代替换行符
  - 确保输出有效的 JSON 格式
  - 转义特殊字符

**修改文件**：
- `ai-sidecar/knowledge/extractor_v2.py` - 更新 SYSTEM_PROMPT

---

## 数据库表结构变化

### knowledge_entries 表

**新增字段**：
- `overview` TEXT - 知识概述
- `details` TEXT - 知识明细
- `occurrence_count` INTEGER - 出现次数（已存在）

**保留字段**：
- `summary` TEXT - 向后兼容

**FTS 索引更新**：
- 从 `(summary, entities)` 更新为 `(overview, details, entities)`

---

## 测试建议

1. **模型管理界面测试**：
   - 打开 Desktop UI 的模型管理页面
   - 确认 Qwen 2.5 (3B) 显示并标记为默认
   - 确认模型状态为"已安装"或"激活中"

2. **知识提炼测试**：
   - 查看新生成的知识条目
   - 确认包含 `overview` 和 `details` 字段
   - 确认 `details` 内容足够详细（200-500字）

3. **知识合并测试**：
   - 在短时间内多次采集相同内容
   - 确认生成单条知识而不是多条
   - 确认 `details` 包含多次采集的补充信息
   - 确认 `occurrence_count` 正确递增

4. **向量搜索测试**：
   - 测试语义相似度搜索
   - 确认相似知识能正确合并
   - 确认不相似的知识独立存储

---

## 回滚方案

如果出现问题，可以回滚：

1. **代码回滚**：
   ```bash
   cd /path/to/MemoryBread
   git checkout HEAD~1 ai-sidecar/
   ```

2. **数据库回滚**：
   - 备份当前数据库：
     ```bash
     cp ~/.memory-bread/memory-bread.db ~/.memory-bread/memory-bread.db.backup
     ```
   - 如需恢复旧表结构，可以删除新字段（但会丢失数据）

---

## 后续优化建议

1. **前端展示优化**：
   - 更新知识库查看界面，分别显示概述和明细
   - 添加"展开/收起"功能查看详细内容
   - 显示知识出现次数和最后更新时间

2. **知识合并策略优化**：
   - 可调整相似度阈值（当前 0.85）
   - 支持手动合并/拆分知识
   - 添加知识版本历史

3. **性能优化**：
   - 限制相似度搜索的范围（当前 1000 条）
   - 使用向量数据库（如 Qdrant）替代实时计算
   - 批量处理知识提炼任务

4. **质量优化**：
   - 添加知识质量评分
   - 支持用户反馈和修正
   - 定期清理低质量知识
