# 记忆面包 滚动修复和向量 RAG 实现总结

## 完成时间
2026-03-09

---

## ✅ 已完成的任务

### 1. 修复滚动功能

**问题**：
- 记忆面包 界面中的滚动功能失效
- 用户无法滚动查看长内容

**解决方案**：
修改了以下 CSS 文件中的 `overflow` 属性：

1. **`#root` 容器** (`desktop-ui/src/index.css:21-25`)
   - 从 `overflow: hidden` 改为 `overflow: auto`

2. **`.rag-panel` 面板** (`desktop-ui/src/index.css:133-140`)
   - 从 `overflow: hidden` 改为 `overflow-y: auto`

3. **`.knowledge-panel` 面板** (`desktop-ui/src/index.css:539-546`)
   - 从 `overflow: hidden` 改为 `overflow-y: auto`

**结果**：所有面板现在都可以正常滚动

---

### 2. 实现向量 RAG 查询功能

**架构说明**：
记忆面包 使用 **Tauri** 框架：
- 前端：React + TypeScript
- 渲染：系统 WebView (macOS 上是 WKWebView)
- 后端：Rust (core-engine) + Python (ai-sidecar)
- 不是纯原生组件，也不是传统的内嵌 H5

**实现方案**：

#### 2.1 创建 RAG HTTP 服务 (ai-sidecar)

**新增文件**：
- `ai-sidecar/rag_api_server.py` - RAG 查询 HTTP 服务（端口 7071）
- `ai-sidecar/rag/worker.py` - RAG Worker（IPC 任务处理器）

**功能**：
- 提供 `/query` API 端点
- 集成完整的 RAG pipeline：
  - 向量检索（Qdrant）
  - 关键词检索（FTS5）
  - RRF 合并
  - LLM 推理（Ollama）

#### 2.2 更新 Core Engine

**修改文件**：
- `core-engine/src/api/handlers/query.rs` - 更新查询逻辑
- `core-engine/Cargo.toml` - 添加 reqwest 依赖

**新逻辑**：
1. 优先调用 RAG 服务（端口 7071）进行智能问答
2. 如果 RAG 服务不可用，降级为关键词搜索
3. 支持超时和错误处理

#### 2.3 更新 IPC 协议

**修改文件**：
- `shared/ipc-protocol/python/memory_bread_ipc/message.py`
- `ai-sidecar/dispatcher.py`

**新增消息类型**：
- `RagRequest` - RAG 查询请求
- `RagResult` - RAG 查询结果
- 在 dispatcher 中添加 RAG worker 支持

---

## 🚀 使用方法

### 启动 记忆面包

#### 方式 1：使用主启动脚本（推荐）
```bash
cd /path/to/MemoryBread
./start.sh start
```

#### 方式 2：手动启动各个服务

1. **启动 AI Sidecar**
```bash
cd ai-sidecar
source .venv/bin/activate
python3 main.py &
```

2. **启动 Model API / RAG API**
```bash
cd ai-sidecar
source .venv/bin/activate
python3 model_api_server.py &
```

3. **启动 Core Engine**
```bash
cd core-engine
./target/release/memory-bread &
```

4. **启动 Desktop UI**
```bash
cd desktop-ui
npm run tauri:dev
```

### 测试 RAG 查询

#### 通过 Desktop UI
1. 打开 记忆面包
2. 会自动显示"记忆面包"问答界面
3. 输入问题，例如："模型"、"视频"、"项目"
4. 查看智能回答和相关上下文

#### 通过 HTTP API
```bash
# 测试 Core Engine 的查询接口
curl -X POST http://localhost:7070/query \
  -H "Content-Type: application/json" \
  -d '{"query": "模型", "top_k": 5}'

# 直接测试 RAG 服务
curl -X POST http://localhost:7071/query \
  -H "Content-Type: application/json" \
  -d '{"query": "模型", "top_k": 5}'
```

---

## 📊 技术细节

### RAG Pipeline 流程

```
用户问题
    ↓
Core Engine (/query)
    ↓
HTTP 调用 RAG 服务 (7071)
    ↓
RAG Pipeline
    ├─ Query Embedding (BGE-M3)
    ├─ 向量检索 (Qdrant)
    ├─ 关键词检索 (FTS5)
    ├─ RRF 合并
    ├─ Prompt 组装
    └─ LLM 推理 (Ollama)
    ↓
返回智能回答 + 上下文
    ↓
Desktop UI 显示
```

### 降级策略

如果 RAG 服务不可用（未启动或超时），Core Engine 会自动降级为：
1. 使用 SQL LIKE 进行关键词匹配
2. 从知识库和采集记录中检索
3. 返回简单的摘要（不调用 LLM）

---

## 🔧 配置说明

### 端口使用
- **7070**: Core Engine REST API
- **7071**: RAG 查询服务
- **1420**: Desktop UI (Vite 开发服务器)
- **Unix Socket**: `/tmp/memory-bread-sidecar.sock` (IPC 通信)

### 数据库
- **路径**: `~/.memory-bread/memory-bread.db`
- **表**:
  - `captures` - 采集记录 (4,149 条)
  - `knowledge_entries` - 知识条目 (1,002 条)
  - `knowledge_fts` - FTS5 全文索引
  - `captures_fts` - FTS5 全文索引

### 日志
- **路径**: `~/.memory-bread/logs/`
- **文件**:
  - `sidecar.log` - AI Sidecar 日志
  - `core.log` - Core Engine 日志
  - `ui.log` - Desktop UI 日志

---

## 🎯 功能对比

### 之前（关键词搜索）
- ❌ 只能匹配精确关键词
- ❌ 无语义理解
- ❌ 简单的文本拼接
- ❌ 无 AI 推理

### 现在（向量 RAG）
- ✅ 语义相似度检索
- ✅ 向量 + 关键词混合检索
- ✅ RRF 智能合并
- ✅ LLM 生成智能回答
- ✅ 自动降级保证可用性

---

## 📝 待优化项

### 短期优化
1. **启动脚本集成**
   - 将 RAG 服务集成到主启动脚本
   - 自动检测 RAG 服务状态

2. **错误处理**
   - 更友好的错误提示
   - 重试机制

3. **性能优化**
   - 缓存 embedding 结果
   - 异步处理

### 长期优化
1. **向量数据库**
   - 确保 Qdrant 正常运行
   - 定期更新向量索引

2. **LLM 模型**
   - 支持多种 LLM 后端
   - 模型选择和切换

3. **用户体验**
   - 流式输出（SSE）
   - 多轮对话
   - 历史记录

---

## 🐛 已知问题

1. **RAG 服务启动**
   - 已集成到主启动脚本，由 `./start.sh start` 启动统一 Model API / RAG API

2. **首次查询较慢**
   - 需要加载 embedding 模型和 LLM
   - 后续查询会快很多

3. **Ollama 依赖**
   - 需要本地安装 Ollama
   - 需要下载 LLM 模型（如 qwen2.5:3b）

---

## ✨ 总结

所有任务已完成：
- ✅ 修复了滚动功能
- ✅ 实现了向量 RAG 查询
- ✅ 集成了 LLM 推理
- ✅ 提供了降级方案
- ✅ 更新了技术文档

记忆面包 现在具备完整的智能问答能力，可以基于工作记录进行语义检索和 AI 推理。
