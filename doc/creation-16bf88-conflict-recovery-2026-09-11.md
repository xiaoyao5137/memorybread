# 第 158 条创作：冲突复核修复与完整恢复验证

## 交付结果

会话 `creation-1789061087358-0aaa4dcf16bf88` 的真实断点副本已完成生产 Agent 的生成、修订与最终验收，返回 `run.completed`。最终报告 12 项检查全部通过，正文 1775 字符，修订计数仍为 2，没有重置或增加全局修订额度。

本次使用只读数据库和隔离断点，不覆盖用户原会话。历史记录 158 的哈希前后相同，原先保存的 3585 字符正文与失败记录保留。这里验证的是实际 Agent、实际生成内容和实际模型审计；没有冒充客户端点击后已经覆盖原文。

## 故障证据

原日志定位到 `_resolve_source_audit_conflict` 的“结论或字段无效”分支。该分支混合了字段错误、合法 uncertain 和超过 240 字的理由，原始响应未落盘，因此不能进一步断言当次命中了哪一项。

旧代码的真实重放另复现了逐片段引文末尾多一个换行，连续两次格式恢复仍失败，最终报“无法核验最终交付结果”。后续完整重放又暴露了冗余整体状态与逐项结果矛盾，以及来源检查的失败引用限本批、通过引用却可跨批的问题。

旧断点的验收条件还把历史参考中的 20 项决定提升成了本轮强制条件。本轮有效确认只有 3 项，旧渲染摘要又省略部分选项说明，导致检查器重复否定用户已经确认的内容。

## 最终修复

- 冲突复核保留完整非空理由；uncertain 作为未通过意见进入正常修订，不当作协议异常，也不直接放行。真正的字段或绑定错误只允许一次格式恢复。
- 来源和候选引文使用有原文位置及片段归属的引用 ID，避免自由抄写引文；旧记录仍严格验证原文，只允许唯一定位时消除边界空白。
- 内部整体状态由逐项结果计算，公开报告继续校验状态一致性。来源检查的通过和失败引用都绑定本批改动，防止批外引用被解码约束强制变成通过。
- 当前结构化决定和选项说明作为审计依据，历史摘要保留为参考，不能覆盖当前确认状态。旧断点重新建立验收条件，保留已完成资料工作和修订次数。
- 选择覆盖失败的小规模方案使用逐项章节修订，保留用户确认原文，区分拟安排与既有事实；未知输入、数据获取渠道和效果保留核实状态。叙事和普通文档不强制改成方案格式。
- 对常见带单位的新数值限定在章节保存前作确定性检查并有限修正，保留用户给定参数及可识别的等值写法，不把步骤编号当成业务阈值。
- 方案覆盖核验依据当前轮次、唯一绑定实际正文的章节，按输入、执行、产物和验证分别检查，再汇总选择结果。章节存在本身不代表通过，任一维度不足仍须修订。

## 验证

- 836 项相关测试通过，覆盖冲突恢复、引用与批次绑定、来源状态、章节写作、覆盖核验、实际 Agent 调度和恢复边界。
- 10 个相关 Python 文件通过 Python 3.9 AST、注解联合类型检查及 Python 3.9 编译；差异检查通过。
- 最终真实恢复耗时 454.22 秒，14 次模型接口调用：仅输入评估和产物形式判断复用完全相同请求的真实响应，全部正文写作、写作修正与交付审计为本次 12 次真实调用。
- 最终正文人工阅读对照了三个确认选择、三个未答事项、未确认量化限定及数据获取状态；三个选择都有实际执行内容，未答事项未被当成用户决定。
- 当前托管 Creation Service 已加载最终源码，PID `21390`，`/health` 返回 `status=ok`。未打包 DMG。

## 本机证据

- 最终完整重放：`/tmp/creation-16bf88/conditional-recovery/result.json`
- 逐事件轨迹：`/tmp/creation-16bf88/conditional-recovery/events.jsonl`
- 通过的验收报告：`/tmp/creation-16bf88/conditional-recovery/review.json`
- 通过的测试正文：`/tmp/creation-16bf88/conditional-recovery/generated.md`
- 原断点及真实待修订断点：`/tmp/creation-16bf88/checkpoint.json`、`/tmp/creation-16bf88/pre-repair-checkpoint.json`
- 回归输出：`/tmp/creation-16bf88/pytest.log`
- 当前服务证据：`/tmp/creation-16bf88/runtime-verification.json`

此前返回 revise、协议失败或被终止的实验都不计为通过。模型语义检查仍可能误判；本次交付依据为真实完成事件、完整逐项报告和实际正文对照，不能据此宣称所有未来生成必定通过。
