# 初始化端口、下载源与向量模型统一优化方案

**状态：** 评审稿（变更批准后可进入实现）  
**负责人：** 未指定  
**决策人：** 未指定  
**最后更新：** 2026-09-13

## 摘要

当前首次启动依赖一组固定回环端口，下载链路对模型源的切换能力不足，同时初始化会拉取 Ollama 向量模型，而 RAG 默认又加载 SentenceTransformers 向量模型。三者叠加后，用户可能因为本机端口碰撞而无法启动、因为单一模型源长时间无进度而误认为卡死，并为同一语义检索能力下载和维护两套模型。

本方案做出三项架构决策：

1. 由 Tauri 本机服务监督器统一管理“逻辑服务名到本次实例端点”的注册表，所有应用内 HTTP 服务改为动态回环端口；端口冲突只触发重新分配，不再成为初始化失败原因。
2. 将运行时和模型下载统一到版本化、可校验、可续传、可切源的制品清单；中国大陆可访问的 MemoryBread 自有对象存储/CDN 为首选源，官方源为可信回退，现有公共 GitHub 代理仅作为带固定哈希校验的应急源。
3. 统一使用 `BAAI/bge-small-zh-v1.5` SentenceTransformers CPU 后端作为唯一向量模型；移除初始化对 Ollama 向量模型的拉取和探针，所有向量生产者消费同一模型指纹契约。

本方案不修改用户安装的 Python 或 Ollama，不终止身份不可验证的外部进程，不删除已有记忆库，也不以随机端口代替进程身份和请求鉴权。

## 问题与证据

### 固定端口把环境差异变成启动失败

当前代码在前端、Tauri、Rust Core、Python Sidecar 和脚本中直接引用 `7070`、`7071`、`7072`、`8001`、`11434` 和 Windows IPC 的 `17071`。初始化器在 `7070` 或 `11434` 被未知进程占用时返回 `CORE_PORT_CONFLICT` 或 `RUNTIME_START_FAILED`。这种行为保护了外部进程，却把用户本机合法软件的端口使用转化成了 MemoryBread 的阻断。

当前工作区检索到 200 多处端口或端点相关引用，说明局部替换某一个端口不能解决问题。必须建立统一端点发现契约，否则前端、服务间调用、浏览器桥接和诊断仍会漂移。

### 下载源和等待预算没有形成统一契约

Ollama CLI 运行时已有 GitHub 官方源和两个公共代理候选，并通过固定 SHA-256 校验。两个 Ollama 模型仍由本机 Ollama `/api/pull` 直接访问默认 registry，没有由 MemoryBread 控制的备用源。模型拉取单次读取超时为 3600 秒，阶段内最多重试三次，自动修复还可能再次执行，弱网络下可能数小时没有最终结果。

SentenceTransformers 模型已有 ModelScope、hf-mirror 和 Hugging Face 级联，但使用可变的 `master`/`main` 路径，没有随客户端发布固定 revision 和逐文件哈希，也没有纳入初始化状态机的统一进度、超时和错误码。

### 两套向量模型重复且契约不一致

初始化阶段拉取 `qllama/bge-small-zh-v1.5:q4_k_m` 并通过 Ollama API 做向量探针；RAG 和创作默认通过 SentenceTransformers 加载 `BAAI/bge-small-zh-v1.5`。两者虽然同属 BGE Small 中文模型且维度均为 512，但量化方式、运行后端、预处理和模型文件指纹不同，不能仅凭名称或维度假设向量可混用。

继续保留 Ollama 向量后端会与 `OLLAMA_MAX_LOADED_MODELS=1` 冲突：文本生成模型和向量模型可能反复换入换出。SentenceTransformers CPU 后端是当前 RAG 默认路径，能够独立于文本生成模型运行，因此本方案选择它作为唯一后端。

## 目标与成果

- 任意一个历史固定端口被其他合法进程占用时，正式安装版仍能完成本机服务启动和首次初始化。
- 用户不需要关闭、升级或卸载自己的 Ollama；MemoryBread 继续使用独立、固定版本、可验证身份的托管运行时。
- 每个网络制品都有固定版本、大小和哈希，单源无进度后会切换来源，所有来源失败时在明确总预算内进入可重试失败态。
- 初始化只下载一套向量模型，初始化质检、RAG、创作、后台提炼和向量重建使用同一模型指纹。
- 升级用户的历史向量和旧 Ollama 向量模型可安全迁移，不覆盖历史数据，不在回滚窗口内提前删除旧制品。

## 非目标

- 不安装或升级用户的系统 Python、Homebrew 或 Ollama.app。
- 不复用身份不可验证的外部 Ollama 服务，也不扫描、接管或终止外部进程。
- 不把本地服务开放到局域网地址；所有监听继续限制在 `127.0.0.1` 或 Unix Domain Socket。
- 不在本次变更中替换文本生成模型 `qwen3.5:4b`。
- 不承诺第三方公共代理的可用性；它们只能作为经固定哈希约束的末级应急来源。
- 不把端口随机化当作本机 API 的唯一安全措施。

## 用户与场景

### 首次安装用户

- 本机已有程序占用一个或多个历史固定端口时，点击初始化后无需理解端口概念，应用自动选择可用端点并继续。
- 中国大陆网络无法访问 GitHub 或 Ollama registry 时，下载器自动切换至可访问的受控镜像，并保留已验证的下载进度。
- 下载确实无法完成时，页面显示当前制品、已尝试来源数量、下一步操作和稳定错误码，不长期停留在没有变化的百分比。

### 升级用户

- 已完成初始化的用户升级后，应用复用已有文本模型和数据库，只补齐新向量模型制品或重建不兼容的向量索引。
- 旧向量索引在新索引完整可用前继续保留；失败时回到旧客户端版本仍可读取原数据。

### 开发与支持人员

- `start.sh` 和正式 Tauri 运行时使用同一逻辑服务名和端点清单，不再维护两套固定 URL。
- 诊断能够区分端点分配失败、进程启动失败、下载连接失败、下载无进度、校验失败和所有来源耗尽。

## 范围

**范围内**

- Core、Model API、Creation、内部向量检索、托管 Ollama 及 Windows TCP IPC 的动态端点。
- Tauri 到前端、服务到服务、浏览器桥接和开发脚本的端点发现。
- Ollama CLI、文本模型和唯一向量模型的统一下载制品清单。
- SentenceTransformers 唯一向量后端、模型指纹、历史索引迁移和旧制品清理。
- 初始化页面的进度、自动切源、失败恢复和诊断事件。

**范围外**

- 云端 API、官网和运营台的公网端口。
- 用户手工添加的云端模型或非默认本地模型。
- App Store 对可下载可执行制品的审核结论；该渠道必须单独走合规门禁。

## 方案决策

### 决策一：逻辑服务端点注册表

端口不再是业务契约。稳定契约是逻辑服务名：

- `core`
- `model_api`
- `creation`
- `vector_search`
- `ollama`
- `ipc_tcp`（仅不支持 Unix Domain Socket 的平台）

Tauri 是正式安装版的唯一监督器。每次 App 实例启动生成新的 `instance_id`、端点 generation 和 256 位本机会话令牌。监督器维护权限为 `0600` 的原子注册表，并通过 Tauri command 向 WebView 返回经过白名单过滤的端点；前端不得直接读取文件。

```json
{
  "schema_version": "local-services.v1",
  "instance_id": "uuid",
  "generation": 12,
  "services": {
    "core": {
      "scheme": "http",
      "host": "127.0.0.1",
      "port": 49152,
      "pid": 1234,
      "create_time": 1789230000,
      "health_path": "/health",
      "status": "ready"
    }
  }
}
```

注册表必须满足以下不变量：

1. `host` 只能是回环地址；客户端拒绝其他地址。
2. 一个 generation 内逻辑服务名唯一、端口不重复。
3. `ready` 必须同时绑定 PID、进程创建时间、预期可执行文件、实例身份和健康响应；端口可连接不等于服务可用。
4. 文件先写临时文件、`fsync` 后原子替换；旧 generation 不得覆盖新 generation。
5. 会话令牌单独存放且仅传给受管进程；诊断和日志不得记录令牌。

### 决策二：动态绑定与冲突恢复

MemoryBread 自有的 Rust/Python HTTP 服务必须支持绑定 `127.0.0.1:0`，由操作系统在真正 bind 时选择端口；服务监听成功后再原子上报实际端点，避免“先探测空闲、释放、随后被抢占”的竞态。

托管 Ollama 当前不能由 MemoryBread 改造其 socket 绑定流程，因此采用监督器候选端口和启动验证机制：

1. 从操作系统临时分配端口候选并建立本次实例租约。
2. 在启动 Ollama 前释放该候选的预留 socket，立即以 `OLLAMA_HOST=127.0.0.1:<candidate>` 启动托管可执行文件。
3. 在 5 秒内验证受管 PID、创建时间、可执行文件、`OLLAMA_MODELS`、监听地址和 `/api/tags`。
4. 若进程明确因 `EADDRINUSE` 退出或端口被另一身份占用，保留诊断事件并换新候选；最多尝试 20 个候选且总计不超过 30 秒。
5. 只有操作系统无法分配任何回环端点或受管进程在不同候选上持续出现非端口启动错误时才失败。后者必须报告真实启动错误，不能伪装为端口冲突。

动态端点不会跨 App 会话承诺稳定。所有消费者必须按逻辑服务名解析，并在 `local-services-changed` 事件或 generation 变化后清空旧连接池、重新取址。

浏览器扩展不扫描端口范围。浏览器桥接进程从受控注册表读取 `core` 端点，并通过现有扩展握手返回当前实例可用状态；扩展请求必须携带短期握手凭据。开发模式由一个共享启动协调器生成同格式注册表和环境变量，固定端口仅保留为显式测试覆盖参数。

### 决策三：可验证的多源制品清单

随客户端发布并签名的 `artifact-manifest.v2` 是下载事实源。每个制品必须固定：

- `artifact_id`、语义版本、目标架构和格式；
- 解压前大小、解压后上限、完整 SHA-256；
- 模型 revision、逐文件路径、大小和 SHA-256；
- 有序 `source_id` 列表和允许的 HTTPS 主机；
- 缓存兼容键和安装后验证方式。

推荐来源顺序如下：

| 制品 | 首选 | 第二来源 | 末级回退 |
| --- | --- | --- | --- |
| Ollama CLI | MemoryBread 中国大陆对象存储/CDN 的固定版本副本 | GitHub Releases 官方固定版本 | `ghfast.top`、`gh-proxy.com`，仅在完整 SHA-256 匹配时接受 |
| `qwen3.5:4b` 精确 GGUF/Ollama 制品 | MemoryBread 中国大陆对象存储/CDN 的固定哈希模型包 | Ollama 官方 registry | 经发布审批的另一受控镜像 |
| `BAAI/bge-small-zh-v1.5` | MemoryBread 中国大陆对象存储/CDN 的固定 revision 文件集 | ModelScope 对应固定 revision | Hugging Face 固定 commit；hf-mirror 仅作为应急源 |

MemoryBread 模型包应封装当前受管 Ollama 所需的 manifest 和 blobs，下载器校验后原子导入专属模型目录；这样首选镜像失败时仍可回退到官方 `/api/pull`，但不会把 Ollama registry 作为唯一入口。模型再分发必须在上线前通过许可证和来源归属审核。

每个来源使用连接超时 10 秒、连续 45 秒无新增字节即切源、单源最多 2 次、单制品总预算 30 分钟。HTTP Range 续传必须绑定 ETag/Last-Modified 和制品哈希；不同 URL 的未验证分片不得拼接。总预算耗尽后进入 `ARTIFACT_SOURCES_EXHAUSTED`，保留可安全续传的缓存并允许用户手动重试。

下载状态机统一为：

`pending -> resolving_source -> downloading -> verifying -> installing -> ready`

失败分支为：

`downloading -> source_stalled/source_failed -> resolving_source`，所有来源或总预算耗尽后进入 `failed`。

### 决策四：统一为一套 SentenceTransformers 向量模型

统一契约使用用户不可见的稳定能力 ID `mbemb-v2-local`，当前实现绑定：

- 模型：`BAAI/bge-small-zh-v1.5`
- 后端：SentenceTransformers
- 设备默认值：CPU
- 维度：512
- 归一化：`normalize_embeddings=true`
- revision：由 `artifact-manifest.v2` 固定
- 指纹：模型文件哈希、tokenizer 哈希、池化配置、归一化标志和预处理版本的规范化 SHA-256

初始化的 `vector_model` 阶段改为通过统一下载器准备该固定 revision。质量门禁和功能探针必须调用共享 `EmbeddingModel` 实际编码固定文本，验证维度、有限数值、归一化和确定性容差。不得再以 Ollama `/api/embed` 成功代替应用真实向量路径的验收。

以下路径必须统一消费 `mbemb-v2-local`：RAG 查询、采集向量、文档分块向量、创作召回、后台重建和初始化探针。移除默认安装清单中的 `qllama/bge-small-zh-v1.5:q4_k_m`，移除 Ollama embedding fallback；正式包缺少 SentenceTransformers 运行依赖时明确报 `EMBEDDING_RUNTIME_MISSING`，提示重新安装客户端，不在用户机器上执行 pip。

## 需求

- **FR-001** — 正式 App 每次启动时，Tauri 必须为所有本机逻辑服务创建同一 `instance_id` 下的版本化端点注册表。
- **FR-002** — MemoryBread 自有 HTTP 服务必须绑定 `127.0.0.1:0` 并在监听成功后上报实际端点。
- **FR-003** — 托管 Ollama 候选端口发生身份冲突时，监督器必须换用新候选继续启动，且不得终止冲突进程。
- **FR-004** — 前端和服务间调用必须按逻辑服务名解析当前 generation，不能依赖历史固定端口。
- **FR-005** — 浏览器桥接必须通过受控注册表发现 Core，不能扫描本机端口。
- **FR-006** — 开发启动器必须生成与正式运行时同 schema 的端点注册表，并向所有子进程注入一致端点。
- **FR-007** — 初始化下载器必须按签名制品清单依次尝试允许的来源，并在来源无进度或失败时自动切换。
- **FR-008** — 初始化下载器必须复用通过来源标识、validator 和目标哈希绑定的安全分片，不能跨不相容来源拼接。
- **FR-009** — 下载总预算耗尽时，初始化必须进入可重试失败态并展示稳定错误码、已尝试来源数和缓存保留状态。
- **FR-010** — 初始化必须只安装 `mbemb-v2-local` 对应的一套向量模型，不再拉取 Ollama 向量模型。
- **FR-011** — 所有生产向量的路径必须持久化统一向量模型指纹，并拒绝在同一索引中混用不兼容指纹。
- **FR-012** — 升级时必须在新索引通过覆盖检查前保留旧索引，并在失败后继续提供不破坏原数据的恢复入口。
- **FR-013** — 回滚窗口结束且确认没有运行中索引引用后，清理器必须只删除清单明确列出的旧 Ollama 向量模型 blob。
- **FR-014** — 初始化和启动不得安装、升级或修改用户的系统 Python 与外部 Ollama 安装。
- **FR-015** — 初始化页面必须展示当前逻辑阶段、制品下载进度、切源状态和预计剩余预算，不能显示供应商内部名称。
- **FR-016** — 本机 HTTP 请求必须携带当前实例会话令牌，服务必须拒绝缺失、过期或属于其他实例的令牌。

- **NFR-001** — 任意历史固定端口被占用的自动化故障注入中，所有受管服务必须在 30 秒端点分配预算内完成取址，且不得向占用进程发送信号。
- **NFR-002** — 动态服务必须只监听回环地址；任何 `0.0.0.0`、局域网地址或非预期 Unix Socket 路径均阻断启动。
- **NFR-003** — 运行时和模型制品在执行或加载前必须通过清单签名、目标架构、大小上限和 SHA-256 校验。
- **NFR-004** — 下载器必须在连续 45 秒无新增字节时停止等待当前来源，并在单制品 30 分钟总预算内给出完成或失败结果。
- **NFR-005** — 注册表、会话令牌、下载状态和模型安装状态必须原子持久化；进程退出不得留下可被新实例接受的 `ready` 身份。
- **NFR-006** — 诊断不得包含用户内容、完整本机路径、会话令牌、下载签名密钥或供应商凭据。
- **NFR-007** — 客户端内 Python 代码继续以 Python 3.9 语法为兼容基线；正式客户端不得调用系统 Python 或 pip。
- **NFR-008** — 新向量后端在批准的检索回归集上不得低于当前 SentenceTransformers 基线；延迟、峰值内存和召回质量必须分别报告，不能只用模型维度证明兼容。

## 状态、权限与失败行为

### 本机服务状态

每个逻辑服务使用 `allocating -> starting -> ready -> stopping -> stopped`。恢复分支为 `starting -> collision -> allocating`。非端口错误进入 `failed`，保留稳定错误码和受管进程证据。

- `collision` 只表示候选端点已由其他身份占用，不计作一次初始化失败。
- generation 变化后，旧端点立即进入 draining；新请求只能发往新 generation。
- App 异常退出后，新实例不得信任旧 PID；必须重新校验创建时间、可执行文件、环境和监听身份。

### 下载状态

- `ARTIFACT_CONNECT_FAILED`：当前源连接失败，可自动切源。
- `ARTIFACT_SOURCE_STALLED`：45 秒无新增字节，可自动切源。
- `ARTIFACT_HTTP_FAILED`：当前源返回不可恢复 HTTP 状态，可自动切源。
- `ARTIFACT_CHECKSUM_MISMATCH`：删除当前源不可信分片并切源，不执行或加载。
- `ARTIFACT_SOURCES_EXHAUSTED`：所有来源或总预算耗尽，用户可重试或上报诊断。
- `ARTIFACT_INSTALL_FAILED`：已验证制品安装失败，保留旧版本并允许重试。

自动切源不需要用户确认。诊断上报仍必须由用户显式确认。

### 向量迁移状态

`legacy_active -> rebuilding -> validating -> current_active -> legacy_retained -> legacy_removed`

- `rebuilding` 使用独立集合或版本列，不覆盖旧向量。
- `validating` 必须核对记录覆盖、指纹一致性、维度和检索探针。
- 任一检查失败回到 `legacy_active`，保留失败证据和可续跑游标。
- `legacy_removed` 仅在回滚窗口结束且引用计数为零后允许。

## 接口与契约

### Tauri 前端接口

- `get_local_service_endpoints()`：返回当前 `instance_id`、generation，以及前端允许访问的 `core`、`model_api`、`creation` 端点和短期请求凭据。
- `local-services-changed`：端点 generation 改变时发出；前端统一 API client 重新读取端点。
- 前端组件不得保存完整 URL 常量；只允许保存逻辑服务名。

### 子进程环境契约

- `MEMORY_BREAD_INSTANCE_ID`
- `MEMORY_BREAD_ENDPOINT_REGISTRY`
- `MEMORY_BREAD_LOCAL_AUTH_TOKEN_FILE`
- `MEMORY_BREAD_CORE_BIND=127.0.0.1:0`
- `MEMORY_BREAD_MODEL_API_BIND=127.0.0.1:0`
- `MEMORY_BREAD_CREATION_BIND=127.0.0.1:0`
- `MEMORY_BREAD_VECTOR_SEARCH_BIND=127.0.0.1:0`
- `OLLAMA_HOST=127.0.0.1:<supervisor-candidate>`

服务间 URL 由监督器在依赖服务 ready 后注入，或通过共享只读 resolver 按逻辑服务名读取；禁止各模块自行拼接默认端口。

### 向量记录契约

每个持久向量至少绑定：

```json
{
  "embedding_profile_id": "mbemb-v2-local",
  "embedding_fingerprint": "sha256:...",
  "dimension": 512,
  "normalized": true,
  "preprocessing_version": "text-normalization.v1"
}
```

同一检索集合只接受一个 `embedding_fingerprint`。查询指纹与集合指纹不一致时，不得静默检索，必须路由到兼容旧索引或返回 `VECTOR_INDEX_REBUILD_REQUIRED`。

## 验收标准

- **AC-001** — 验证 **FR-001、FR-002、FR-004、FR-016**。给定历史固定端口全部被身份不可验证的测试进程占用，当启动正式 App 测试包时，则 Core、Model API、Creation、Vector Search 均绑定其他回环端口，前端通过 Tauri 返回的同一 generation 完成健康请求，测试进程 PID 和信号记录不变。
- **AC-002** — 验证 **FR-003、FR-014**。给定 11434 和监督器前两个 Ollama 候选端口在启动窗口内被外部进程占用，当初始化推理运行时时，则第三个候选上的 MemoryBread 托管 Ollama 通过完整身份校验，外部 Ollama 文件、版本和进程均未改变。
- **AC-003** — 验证 **FR-004、FR-005**。给定 Core 在运行中由监督器以新 generation 重启到新端点，当前端和浏览器扩展继续请求时，则二者刷新端点后成功，且没有对端口范围执行探测请求。
- **AC-004** — 验证 **FR-006**。给定开发模式为所有历史固定端口注入占用，当执行 `./start.sh restart` 时，则启动器生成 `local-services.v1`，所有开发服务的环境和健康探针与注册表一致。
- **AC-005** — 验证 **FR-007、FR-008**。给定首选源在已下载 40% 后断开、第二源支持相同目标制品但 validator 不同，当继续初始化时，则下载器不拼接不相容字节，从已验证边界重新下载并最终只安装哈希匹配的制品。
- **AC-006** — 验证 **FR-007、FR-009、FR-015**。给定首选源不可连接、第二源连续 45 秒无字节、官方源返回永久 HTTP 错误，当初始化时，则依次记录三次匿名来源尝试，在 30 分钟总预算内返回 `ARTIFACT_SOURCES_EXHAUSTED`，页面展示已尝试来源数和“缓存已保留”。
- **AC-007** — 验证 **FR-010**。给定全新用户目录，当完成冷初始化时，则下载清单和模型目录只包含 `qwen3.5:4b` 与 `mbemb-v2-local` 所需制品，不存在 Ollama embedding manifest，初始化的向量探针通过共享 SentenceTransformers 服务完成。
- **AC-008** — 验证 **FR-011**。给定采集、文档、RAG、创作和后台重建各生成一批向量，当读取持久化元数据时，则所有记录的 profile、指纹、维度、归一化和预处理版本完全一致。
- **AC-009** — 验证 **FR-011、FR-012**。给定包含旧 Ollama 向量指纹的真实结构副本，当执行升级迁移并在中途终止进程时，则旧索引仍可读，新索引从游标续跑，完成覆盖和检索探针前不会切换 active 指针。
- **AC-010** — 验证 **FR-012、FR-013**。给定新索引已激活但仍处于回滚窗口，当执行清理时，则旧索引和旧模型 blob 保留；给定窗口结束且引用计数为零，再执行清理时只删除清单列明的旧向量模型 digest，不删除文本模型或用户模型。
- **AC-011** — 验证 **FR-014**。给定系统没有 Python、Python 低于项目基线，以及分别安装不同版本的外部 Ollama，当运行正式安装包初始化时，则三种环境均不执行 `python`、`pip`、`brew` 或外部 Ollama 升级命令。
- **AC-012** — 验证 **FR-015**。给定下载正在首选源、切换来源和所有来源耗尽三种状态，当使用键盘和屏幕阅读器访问初始化页时，则进度、状态、错误和操作均可感知，页面不出现供应商模型名、镜像 URL 或本机完整路径。

## 度量与可观测性

- **OBS-001** — `local_endpoint_allocation`：记录逻辑服务、候选次数、碰撞次数、耗时和最终状态；不记录外部进程命令行或路径。用于确认端口碰撞不再转化为启动失败。
- **OBS-002** — `managed_service_identity_check`：记录逻辑服务、generation、检查项和稳定错误码；不记录会话令牌。用于区分端口可达和受管服务就绪。
- **OBS-003** — `artifact_source_attempt`：记录 `artifact_id`、匿名 `source_id`、开始/结束字节、耗时、HTTP 状态类别和稳定错误码；客户端 UI 不接收真实源 URL。
- **OBS-004** — `artifact_download_finished`：记录缓存复用字节、总耗时、切源次数、校验结果和安装结果。用于评估来源排序和超时预算。
- **OBS-005** — `embedding_migration_progress`：记录 profile、指纹、待处理数、已处理数、失败数和游标；不记录正文或向量值。
- **OBS-006** — 发布验收分别报告端点分配成功率、初始化完成率、制品下载耗时分位数、向量迁移覆盖率、检索质量、峰值内存和端到端延迟，不以单项通过替代完整验收。

## 发布、迁移与回滚

- **ROLL-001** — 第一阶段先落地端点注册表和统一 resolver，正式包仍允许显式 legacy 固定端点回退；完成所有消费者清单和硬编码扫描门禁后，才能开启动态端点默认值。回滚时恢复 legacy resolver，不修改数据库和模型。
- **ROLL-002** — 第二阶段上线 `artifact-manifest.v2` 和统一下载器，先覆盖 Ollama CLI，再覆盖文本模型，最后覆盖向量模型。任一制品镜像未通过哈希、断点续传和故障切源演练时停止发布；回滚到旧下载器时保留兼容缓存但不接受未验证分片。
- **ROLL-003** — 第三阶段停止新安装拉取 Ollama 向量模型，向量写入切到 `mbemb-v2-local`，升级用户后台构建新索引。新索引未通过覆盖、指纹和真实检索回归前不得切换 active 指针。
- **ROLL-004** — 第四阶段在至少一个完整客户端回滚窗口后启用旧向量制品清理。清理功能独立开关，发现未知引用或 digest 不匹配时立即停止；回滚通过重新启用旧索引和旧客户端完成。
- **ROLL-005** — 每阶段分别完成单元测试、端口并发故障注入、离线/弱网冷初始化、升级数据副本迁移、正式签名 App 和至少一台干净 Mac 验收；源码测试不能替代安装包验收。

## 风险与缓解

- **动态端点传播遗漏。** 某个遗留常量仍访问旧端口，会表现为局部功能失败。通过统一 resolver、禁止新增回环 URL 的静态扫描和服务矩阵端到端测试控制。
- **端点文件被伪造。** 随机端口不提供身份保证。通过 Tauri 所有权、`0600` 文件、instance token、PID 创建时间、可执行文件和健康响应联合校验控制。
- **Ollama 端口存在释放竞态。** 第三方二进制不能接收预绑定 socket。通过短窗口、进程退出原因检查、最多 20 个候选和完整身份校验降低风险；不承诺单一候选必定成功。
- **自有模型镜像的许可证风险。** 未确认再分发权时不能上线模型副本。将许可证审批设为发布门禁，不能用技术哈希校验替代授权。
- **SentenceTransformers 峰值内存高于量化 Ollama embedding。** 单模型减少 Ollama 换模和下载重复，但 CPU 后端仍需独立内存验收。发布报告必须比较空闲、峰值和并发场景；不满足目标设备档位时暂停统一后端发布，而不是静默保留两套默认路径。
- **历史向量不兼容。** 名称和维度相同不能证明向量空间完全兼容。通过显式指纹、双索引迁移、覆盖检查和回滚窗口控制。
- **镜像内容漂移或供应链污染。** 所有可执行文件和模型固定 revision、大小与哈希；校验失败不得加载，镜像不能修改随客户端签名的事实源。

## 待决策问题

- MemoryBread 中国大陆制品源使用哪一套对象存储和 CDN，由发布负责人决定；该选择不改变客户端清单和下载契约。
- `qwen3.5:4b` 与 `BAAI/bge-small-zh-v1.5` 的再分发许可证结论及归属文案需要发布与合规负责人批准，未批准时继续只使用官方源。
- 低内存设备对 SentenceTransformers 唯一后端的最低硬件门槛，需要基于签名安装包实测决定；不得从开发机单次数据推断。
- legacy 固定端点兼容层保留几个客户端版本，需要发布负责人结合回滚策略决定。

## 质量评审

按构建就绪标准自评：问题忠实度 2/2、范围控制 2/2、行为完整性 2/2、测试与追踪 2/2、可运维性 2/2、决策卫生 2/2。镜像承载方、许可证和最低硬件门槛仍是发布门禁，不阻碍端点注册表、统一下载器和向量指纹契约进入工程评审。

## 变更历史

- 2026-09-13 — 首版评审稿；统一提出动态端点、多源校验下载和单一 SentenceTransformers 向量模型方案。
