# 本地记忆检索成稿独立语义复核

结论：**最终交付通过（单个真实模型、隔离 SQLite 样本）**。独立逐字核对了原始指令、召回记录、生成及修复过程与最终正文；结论并非照抄 `passed` 或模型验收。运行记录为 `memory-path-live.json`，耗时 168.95 秒、7 次模型调用。原指令与修复前保存的失败记录逐字相同。

## 指令、来源与最终正文

指令要求读取指定本地材料，仅复述已完成项数、待办项数、负责人并注明资料标题；不加编号、日期、比例、合计、原因、效果或建议；不得转用互联网或看板，缺资料时明确失败。

固定来源原文：`青禾项目验收材料：已完成3项；待办2项；负责人林舟。`

| 核对项 | 固定来源与实际召回 | 最终正文 | 判定 |
| --- | --- | --- | --- |
| 来源身份 | `source_type=document`、`source_id=61001`、标题 `青禾项目验收材料`、`source_url=null`，四项均与 fixture 完全相同 | 标题和“资料标题”准确标明同一材料 | 通过 |
| 已完成项数 | 已完成 3 项 | `已完成项数：3 项` | 通过，未与待办数互换 |
| 待办项数 | 待办 2 项 | `待办项数：2 项` | 通过，未称总数或完成数 |
| 负责人 | 林舟 | `负责人：林舟` | 通过，未扩大职责或增添人物 |
| 禁止新增的信息 | 原文仅上述三字段与标题 | 数字仅为 3、2；没有编号、日期、比例、总计、因果、效果、建议或额外事实 | 通过 |
| “一小段” | 无连续散文、禁止标题或禁止分行的额外要求 | 64 个字符（含 Markdown 与换行），一个标题及以硬换行分隔的简短正文段 | 通过；标签式复述合理，标题重复属于可精简的样式，不构成新增事实 |

最终 `document` 与最后 `run.completed.data.document` 逐字一致：

```markdown
# 《青禾项目验收材料》

**资料标题：** 青禾项目验收材料  
已完成项数：3 项  
待办项数：2 项  
负责人：林舟
```

## 实际检索、隔离与来源真值

核读 `ai-sidecar/scripts/evaluate_creation_memory_path.py`：`RecordingService.retrieve_references` 调用 `super().retrieve_references` 后记录原始返回，并非用固定引用替换检索函数；fixture 通过真实 `sqlite3` 插入 `bake_documents`，其余五个来源域为空。本次实际检索查询为 `读取本地《青禾项目验收材料》`，候选、合格及最终选择均为 1；`retrieval_paths=["keyword"]`、向量召回关闭。唯一引用的 `full_content` 和 `summary` 均逐字等于固定来源；该原文也确实进入首稿模型输入的“本地参考”。`memory_search` 执行完成回执的引用为 `document:61001`，最终引用仍保留该来源。

因此这里证明的是**生产 SQLite 检索实现，在隔离合成数据库上实际执行，并经真实本地模型成稿**。没有读取用户真实生产库，也不是用真实用户资料验证事实。隔离器实际约束 SQLite 连接、DNS 和 socket 连接；记录的 27 次 SQLite 打开均指向 `/tmp/mb-creation-memory-6venrd6n/fixture.sqlite`，7 次网络连接均为本地模型端口 `127.0.0.1:11434`，无越界尝试；工具阶段仅调用记忆搜索，未查询看板或外网。数据库运行前后字节哈希一致，由运行器现场核验；临时数据库随后按设计清理，本复核没有宣称重新打开已清理文件。来源标记 `historical_only/unverified` 不证明材料最新，但本指令也未要求时效核验，成稿没有补充日期或时效声明。

## 修复过程与验收解释边界

首稿错误输出“现有证据未覆盖已完成项数、待办项数和负责人信息，无法生成符合要求的正文内容”。首轮验收整体为 `revise`，经一次正文修复后，第二轮为 `pass` 并完成交付。最终三字段与来源实际一致，正文不再包含错误失败声明，故接受最终交付；不能描述成“首次生成即正确”。

首轮 `doc_access=true` 引用的候选正文只是失败声明，不能支持其所述字段；最终审核的若干单行引用也不足以单独证明所有条件或“未联网”。这是模型审核解释的局限。实际资料取得由独立检索原始返回及工具收据证明，最终内容正确由逐项对照证明，网络边界由隔离记录及代码证明。本报告不将模型的解释或引用本身视为充分证据。

本样本未验证文档缺失时的失败分支、多文档竞争与歧义、向量召回、真实用户数据库或不同请求的稳定成功率；不据此作全场景保证。

## 证据校验值

- 原始运行 JSON SHA-256：`2d76994c5a3112e63b48cd11c7040fdb8dda67411185cdc7ac6ea7983bb700bf`
- 来源正文 SHA-256：`bceaef80016c0fafee578da6d299321756a84d8a487bd887769445dbee047ca5`
- 最终正文 SHA-256：`299ec8a1949f9b54645b34e566f1dc815ad31d0ff5b9ab6c206eeeb4c056a0d8`
- 运行器记录的 fixture 数据库 SHA-256：`cb75191116f81f47f4bee6b61a1ddb1ed7ef5bc2369ba20bf23ca95505473ba1`
