# 完整生成修正的真实输出复核

## 调用方式诊断

[诊断原始记录](repair-diagnostic.json)来自原 v6 的错误稿、同一份可信材料和验收条件。原记录为 `/tmp/mb-creation-qa-20260908/delivery-regenerate-diagnostic-v18.json`；这是一项独立诊断，尚不作为生产修正闭环或完整用户路径通过的证明。

真实输出为：

```markdown
# 会议通知

**时间：**周三下午  
**地点：**二楼会议室  

特此通知，请准时参加。
```

实现代理和主代理分别核对了正文：时间、地点与用户原对话完全一致；没有新增共同工作关系、自然周限定、负责人或报备流程；邀请参加已给定会议属于正常通知表达；句子与格式完整。输出前部明确给出会议时间，通知可以直接发送。没有把生成操作当作具有独立聊天 `response` 字段的接口。

同一次诊断实际执行正文生成及原独立验收，共两次模型调用、83.98 秒，六项检查全部通过。生成使用现有 `stream_agent_document`，8192 token 预算、temperature 0.55、Markdown 输出；此前复杂补丁控制使用 JSON specialist、8192、temperature 0。该证据支持整套调用方式在这个样本上的改善，不能据此推断单独温度、字段顺序或某句提示的因果效果。

原可信材料逐字段保持一致，失败稿只提供失败问题及标题、章节结构参考，不再作为必须保留的全文写作基线。原失败问题与同链历史仍供独立审查核对。模型通过不能替代上述正文人工复核。

## 生产修正入口

[生产控制原始记录](repair-production.json)从原 v6 初次错误稿开始，真正经过 `_execute_step`、Markdown 写入和事件、原独立验收。可信输入、初始错误稿及第一调用的 system/user 提示与上述成功诊断逐字一致，主代理通过脚本再次断言相等。

最终实际正文为：

```markdown
# 会议通知

本次会议定于周三下午在二楼会议室举行，请您届时参加。
```

一次内容修正、一次独立验收，86.73 秒，六项检查全部通过。主代理独立核对：周三下午和二楼会议室准确，“您”及参加邀请没有附加身份或外部流程；没有“本周”、新角色、报备条件或残句。正文前部明确给出会议时间，并形成可直接发送的通知；没有声称另有独立聊天响应字段。

实际 `document.replaced` 事件与最终受审正文逐字一致；该控制使用 `document.preview`，没有将新稿作为 delta 追加到旧稿。没有被旧润色长度保护或局部拼接回退。控制在生产修正与检查节点范围内运行，不把它冒称为完整用户入口的 `run.completed`；原始混合指令完整路径与来源范围正反对照另行验收。

本控制正文 SHA-256：`840756eb93ccc81fc6485bf47f4f11cfa7bfb718848e1f892871f1c2472a6bb6`。

## 原始混合指令完整运行

随后使用原始混合指令、原始会议材料、空正文和空设置完成 v21 全流程，324.08 秒、7 次实际模型调用、一次内容修正，以 `run.completed` 结束。首稿的无依据身份、周次与额外流程被退回，最终正文是“会议时间定于周三下午，地点为二楼会议室。特此通知。”人工核对通过。该记录与上方从失败候选开始的两次调用控制分别保存，见 [完整执行](mixed-user-path.json)及[成品复核](semantic-review.md)。
