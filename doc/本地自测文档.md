# 记忆面包 本地自测文档

> 版本：v1.0 | 日期：2026-03-04
> 本地开发环境搭建与功能测试指南

---

## 一、环境准备

### 1.1 系统要求

| 项目 | 要求 |
|------|------|
| 操作系统 | macOS 12+ / Windows 10+ |
| CPU | Apple Silicon / Intel x64 |
| 内存 | 8GB+ (推荐 16GB) |
| 磁盘空间 | 20GB+ 可用空间 |

### 1.2 必需软件

#### 1.2.1 Rust 工具链

```bash
# 安装 Rust（如果未安装）
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# 验证安装
rustc --version  # 应显示 1.93.1 或更高
cargo --version
```

#### 1.2.2 Python 环境

```bash
# 验证 Python 版本（需要 3.11+）
python3 --version  # 应显示 3.11 或更高

# 创建虚拟环境
cd ai-sidecar
python3 -m venv .venv
source .venv/bin/activate  # macOS/Linux
# .venv\Scripts\activate   # Windows
```

#### 1.2.3 Node.js 环境（用于 UI）

```bash
# 验证 Node.js 版本（需要 18+）
node --version  # 应显示 v18 或更高
npm --version
```

#### 1.2.4 系统权限（macOS）

记忆面包 需要以下系统权限：

1. **辅助功能权限**（Accessibility）
   - 系统偏好设置 → 安全性与隐私 → 辅助功能
   - 添加终端应用（Terminal.app 或 iTerm2）

2. **屏幕录制权限**
   - 系统偏好设置 → 安全性与隐私 → 屏幕录制
   - 添加终端应用

---

## 二、项目构建

### 2.1 克隆项目

```bash
cd /path/to/MemoryBread
```

### 2.2 构建 Shared 模块

#### 2.2.1 IPC Protocol（Rust 端）

```bash
cd shared/ipc-protocol/rust
cargo build --release
cargo test
```

预期输出：
```
running 12 tests
test tests::test_ping_request ... ok
test tests::test_ocr_request ... ok
...
test result: ok. 12 passed; 0 failed
```

#### 2.2.2 IPC Protocol（Python 端）

```bash
cd shared/ipc-protocol/python
source ../../ai-sidecar/.venv/bin/activate
pip install -e .
```

验证安装：
```bash
python -c "from memory_bread_ipc import IpcRequest; print('OK')"
```

### 2.3 构建 Core Engine

```bash
cd core-engine
cargo build --release
```

预期输出：
```
   Compiling memory-bread-core v0.1.0
    Finished release [optimized] target(s) in 2m 34s
```

运行测试：
```bash
cargo test
```

预期输出：
```
running 66 tests
test capture::tests::test_capture_event_mapping ... ok
test storage::tests::test_db_migrations ... ok
test api::tests::test_health_endpoint ... ok
...
test result: ok. 66 passed; 0 failed
```

### 2.4 构建 AI Sidecar

```bash
cd ai-sidecar
source .venv/bin/activate
pip install -r requirements.txt
```

安装的主要依赖：
- `paddleocr` - OCR 引擎
- `sentence-transformers` - Embedding 模型
- `qdrant-client` - 向量数据库客户端
- `pytest` - 测试框架

运行测试：
```bash
pytest -v
```

预期输出：
```
tests/test_ocr.py::test_ocr_engine_init PASSED
tests/test_ocr.py::test_ocr_recognize PASSED
tests/test_embedding.py::test_embedding_model PASSED
...
========== 59 passed in 12.34s ==========
```

### 2.5 构建 Desktop UI

```bash
cd desktop-ui
npm install
npm run build
```

---

## 三、启动服务

### 3.1 启动顺序

记忆面包 由 3 个独立进程组成，需按以下顺序启动：

```
1. AI Sidecar (Python)  ← 先启动，监听 IPC 连接
2. Core Engine (Rust)   ← 连接 Sidecar，启动采集和 API
3. Desktop UI (Tauri)   ← 连接 Core Engine API
```

### 3.2 启动 AI Sidecar

```bash
cd ai-sidecar
source .venv/bin/activate
python -m dispatcher
```

预期输出：
```
[2026-03-04 10:00:00] INFO - AI Sidecar 启动中...
[2026-03-04 10:00:01] INFO - IPC Server 监听: /tmp/memory-bread-sidecar.sock
[2026-03-04 10:00:02] INFO - 等待 Core Engine 连接...
```

**首次启动会下载模型**（约 2-5 分钟）：
- PaddleOCR 模型（~10MB）
- BGE-M3 Embedding 模型（~2GB）

### 3.3 启动 Core Engine

**新开一个终端窗口**：

```bash
cd core-engine
cargo run --release
```

预期输出：
```
[2026-03-04 10:00:05] INFO - 记忆面包 Core Engine 启动中...
[2026-03-04 10:00:06] INFO - 连接 AI Sidecar: /tmp/memory-bread-sidecar.sock
[2026-03-04 10:00:06] INFO - Sidecar 连接成功 (ping: 2ms)
[2026-03-04 10:00:07] INFO - 数据库初始化: ~/.memory-bread/memory-bread.db
[2026-03-04 10:00:07] INFO - 运行迁移: 001_init.sql, 002_fts.sql, 003_vector.sql
[2026-03-04 10:00:08] INFO - REST API 启动: http://localhost:7070
[2026-03-04 10:00:08] INFO - 采集引擎已启动（事件驱动模式）
```

### 3.4 启动 Desktop UI

**新开第三个终端窗口**：

```bash
cd desktop-ui
npm run tauri dev
```

预期输出：
```
[2026-03-04 10:00:10] INFO - Tauri 开发服务器启动...
[2026-03-04 10:00:12] INFO - 前端: http://localhost:5173
[2026-03-04 10:00:13] INFO - 连接 Core Engine: http://localhost:7070
[2026-03-04 10:00:13] INFO - 健康检查通过 ✓
```

此时会弹出 记忆面包 主窗口。

---

## 四、功能测试

### 4.1 基础功能测试

#### 4.1.1 健康检查

```bash
curl http://localhost:7070/health
```

预期响应：
```json
{
  "status": "ok",
  "version": "0.1.0",
  "sidecar_connected": true,
  "db_path": "/path/to/user-home/.memory-bread/memory-bread.db"
}
```

#### 4.1.2 采集功能测试

**测试步骤**：
1. 切换到浏览器窗口
2. 打开一个网页（如 https://github.com）
3. 等待 2-3 秒
4. 查询采集记录

```bash
curl "http://localhost:7070/api/captures?limit=5"
```

预期响应：
```json
{
  "captures": [
    {
      "id": 1,
      "timestamp": 1709510400000,
      "event_type": "app_switch",
      "app_name": "Google Chrome",
      "window_title": "GitHub: Let's build from here",
      "screenshot_path": "/path/to/user-home/.memory-bread/captures/1709510400_app_switch.jpg",
      "ocr_text": "GitHub\nLet's build from here...",
      "ax_tree_json": "{...}"
    }
  ],
  "total": 1
}
```

#### 4.1.3 OCR 功能测试

```bash
# 手动触发 OCR（通过 IPC）
curl -X POST http://localhost:7070/api/ocr \
  -H "Content-Type: application/json" \
  -d '{
    "image_path": "/path/to/user-home/.memory-bread/captures/1709510400_app_switch.jpg"
  }'
```

预期响应：
```json
{
  "text": "GitHub\nLet's build from here\nThe complete developer platform...",
  "confidence": 0.95,
  "latency_ms": 180
}
```

#### 4.1.4 全文搜索测试

```bash
curl "http://localhost:7070/api/search?q=GitHub&limit=10"
```

预期响应：
```json
{
  "results": [
    {
      "id": 1,
      "timestamp": 1709510400000,
      "app_name": "Google Chrome",
      "window_title": "GitHub: Let's build from here",
      "snippet": "...GitHub Let's build from here...",
      "score": 0.89
    }
  ],
  "total": 1
}
```

### 4.2 隐私过滤测试

#### 4.2.1 密码框过滤

**测试步骤**：
1. 打开任意登录页面（如 https://github.com/login）
2. 点击密码输入框
3. 输入一些字符
4. 查询采集记录

```bash
curl "http://localhost:7070/api/captures?limit=1"
```

**预期结果**：
- 不应有新的采集记录
- 或者 `ocr_text` 和 `ax_tree_json` 字段为空

#### 4.2.2 黑名单应用过滤

**测试步骤**：
1. 打开 1Password 应用（如果已安装）
2. 切换窗口
3. 查询采集记录

**预期结果**：
- 不应有 1Password 相关的采集记录

### 4.3 键鼠执行测试

#### 4.3.1 鼠标点击

```bash
curl -X POST http://localhost:7070/api/execute \
  -H "Content-Type: application/json" \
  -d '{
    "action": "mouse_click",
    "x": 100,
    "y": 200
  }'
```

**预期结果**：
- 鼠标光标移动到 (100, 200)
- 执行点击操作

#### 4.3.2 键盘输入

```bash
curl -X POST http://localhost:7070/api/execute \
  -H "Content-Type: application/json" \
  -d '{
    "action": "type_text",
    "text": "Hello 记忆面包"
  }'
```

**预期结果**：
- 在当前焦点位置输入 "Hello 记忆面包"

#### 4.3.3 快捷键

```bash
curl -X POST http://localhost:7070/api/execute \
  -H "Content-Type: application/json" \
  -d '{
    "action": "hotkey",
    "keys": ["cmd", "c"]
  }'
```

**预期结果**：
- 执行 Cmd+C 复制操作

### 4.4 RAG 语义搜索测试

#### 4.4.1 向量化采集记录

```bash
curl -X POST http://localhost:7070/api/vectorize \
  -H "Content-Type: application/json" \
  -d '{
    "capture_id": 1
  }'
```

预期响应：
```json
{
  "status": "ok",
  "vector_id": "vec_1709510400",
  "dimension": 1024
}
```

#### 4.4.2 语义搜索

```bash
curl -X POST http://localhost:7070/api/rag/search \
  -H "Content-Type: application/json" \
  -d '{
    "query": "我最近看过的代码仓库",
    "limit": 5
  }'
```

预期响应：
```json
{
  "results": [
    {
      "capture_id": 1,
      "app_name": "Google Chrome",
      "window_title": "GitHub: Let's build from here",
      "timestamp": 1709510400000,
      "score": 0.87
    }
  ]
}
```

### 4.5 UI 功能测试

#### 4.5.1 时间线视图

1. 打开 记忆面包 主窗口
2. 点击"时间线"标签
3. 应显示按时间倒序的采集记录列表
4. 点击任意记录，应显示详情（截图 + OCR 文本 + AX 树）

#### 4.5.2 搜索功能

1. 在搜索框输入关键词（如 "GitHub"）
2. 应实时显示匹配结果
3. 支持全文搜索和语义搜索切换

#### 4.5.3 设置页面

1. 点击"设置"按钮
2. 测试以下设置项：
   - 采集频率（默认：事件驱动）
   - 截图质量（默认：80%）
   - 数据保留天数（默认：90 天）
   - 黑名单应用列表
   - 隐私模式开关

---

## 五、性能测试

### 5.1 CPU 占用测试

```bash
# macOS
top -pid $(pgrep -f "memory-bread-core")

# 预期：CPU < 10%（后台采集状态）
```

### 5.2 内存占用测试

```bash
# macOS
ps aux | grep memory-bread

# 预期：
# - Core Engine: < 500MB
# - AI Sidecar: < 1.5GB（模型加载后）
# - Desktop UI: < 200MB
```

### 5.3 磁盘增长测试

```bash
# 运行 1 小时后检查数据目录大小
du -sh ~/.memory-bread

# 预期：
# - 数据库: ~5-10MB
# - 截图: ~50-100MB（取决于采集频率）
```

### 5.4 响应延迟测试

```bash
# OCR 延迟
time curl -X POST http://localhost:7070/api/ocr \
  -H "Content-Type: application/json" \
  -d '{"image_path": "..."}'

# 预期：< 200ms

# RAG 检索延迟
time curl -X POST http://localhost:7070/api/rag/search \
  -H "Content-Type: application/json" \
  -d '{"query": "测试查询", "limit": 10}'

# 预期：< 500ms
```

---

## 六、常见问题排查

### 6.1 AI Sidecar 无法启动

**症状**：
```
[ERROR] Failed to bind socket: Address already in use
```

**解决方案**：
```bash
# 检查端口占用
lsof -i :17071  # Windows
lsof /tmp/memory-bread-sidecar.sock  # macOS

# 杀死占用进程
kill -9 <PID>

# 或删除 socket 文件
rm /tmp/memory-bread-sidecar.sock
```

### 6.2 Core Engine 连接 Sidecar 失败

**症状**：
```
[ERROR] Failed to connect to Sidecar: Connection refused
```

**解决方案**：
1. 确认 AI Sidecar 已启动
2. 检查 socket 文件权限
```bash
ls -la /tmp/memory-bread-sidecar.sock
# 应显示 srwxr-xr-x
```

### 6.3 截图权限被拒绝（macOS）

**症状**：
```
[ERROR] Screenshot failed: Permission denied
```

**解决方案**：
1. 系统偏好设置 → 安全性与隐私 → 屏幕录制
2. 添加终端应用（Terminal.app）
3. 重启终端和 Core Engine

### 6.4 Accessibility 权限被拒绝（macOS）

**症状**：
```
[ERROR] AX API failed: kAXErrorAPIDisabled
```

**解决方案**：
1. 系统偏好设置 → 安全性与隐私 → 辅助功能
2. 添加终端应用
3. 重启终端和 Core Engine

### 6.5 OCR 识别失败

**症状**：
```
[ERROR] OCR failed: Model not loaded
```

**解决方案**：
```bash
cd ai-sidecar
source .venv/bin/activate

# 手动下载 PaddleOCR 模型
python -c "from paddleocr import PaddleOCR; PaddleOCR(use_angle_cls=True, lang='ch')"
```

### 6.6 数据库锁定错误

**症状**：
```
[ERROR] Database is locked
```

**解决方案**：
```bash
# 检查是否有多个 Core Engine 实例
ps aux | grep memory-bread-core

# 杀死多余进程
kill -9 <PID>

# 或删除 WAL 文件
rm ~/.memory-bread/memory-bread.db-wal
rm ~/.memory-bread/memory-bread.db-shm
```

---

## 七、开发调试

### 7.1 启用详细日志

#### Core Engine（Rust）
```bash
RUST_LOG=debug cargo run
```

#### AI Sidecar（Python）
```bash
export LOG_LEVEL=DEBUG
python -m dispatcher
```

### 7.2 单元测试

#### Rust 测试
```bash
cd core-engine

# 运行所有测试
cargo test

# 运行特定模块测试
cargo test capture::tests

# 显示测试输出
cargo test -- --nocapture
```

#### Python 测试
```bash
cd ai-sidecar

# 运行所有测试
pytest -v

# 运行特定文件测试
pytest tests/test_ocr.py -v

# 显示详细输出
pytest -v -s
```

### 7.3 集成测试

```bash
cd core-engine

# 运行集成测试（需要 Sidecar 运行）
cargo test --test api_tests
```

### 7.4 性能分析

#### Rust Profiling
```bash
cargo install flamegraph
cargo flamegraph --bin memory-bread-core
```

#### Python Profiling
```bash
pip install py-spy
py-spy record -o profile.svg -- python -m dispatcher
```

---

## 八、生产部署准备

### 8.1 构建 Release 版本

```bash
# Core Engine
cd core-engine
cargo build --release
strip target/release/memory-bread-core  # 减小二进制大小

# Desktop UI
cd desktop-ui
npm run tauri build
```

### 8.2 打包分发

#### macOS
```bash
cd desktop-ui/src-tauri/target/release/bundle/macos
# 生成 记忆面包.app
```

#### Windows
```bash
cd desktop-ui/src-tauri/target/release/bundle/msi
# 生成 记忆面包.msi
```

### 8.3 首次运行配置

用户首次启动时，应引导完成：
1. 系统权限授予（Accessibility + 屏幕录制）
2. 数据存储位置选择（默认 `~/.memory-bread`）
3. 隐私设置（黑名单应用、敏感 URL）
4. AI 模型下载（可选离线包）

---

## 九、测试检查清单

### 9.1 功能测试

- [ ] 应用切换触发采集
- [ ] 鼠标点击触发采集
- [ ] 键盘输入触发采集（2 秒停顿）
- [ ] 滚动触发采集
- [ ] 截图正常保存
- [ ] OCR 识别准确
- [ ] Accessibility Tree 抓取完整
- [ ] 密码框自动跳过
- [ ] 黑名单应用过滤
- [ ] 全文搜索返回正确结果
- [ ] 语义搜索返回相关结果
- [ ] 鼠标点击执行正确
- [ ] 键盘输入执行正确
- [ ] 快捷键执行正确
- [ ] UI 时间线显示正常
- [ ] UI 搜索功能正常
- [ ] UI 设置保存生效

### 9.2 性能测试

- [ ] CPU 占用 < 10%（后台）
- [ ] 内存占用 < 2GB（总计）
- [ ] 单日数据增量 < 100MB
- [ ] OCR 延迟 < 200ms
- [ ] RAG 检索延迟 < 500ms
- [ ] 键鼠执行延迟 < 1s

### 9.3 稳定性测试

- [ ] 连续运行 24 小时无崩溃
- [ ] 采集 1000+ 记录无性能下降
- [ ] 数据库 VACUUM 正常执行
- [ ] 截图清理正常执行
- [ ] Sidecar 重启后自动重连
- [ ] 网络断开不影响本地功能

---

## 十、下一步开发

根据 `生成顺序.md`，当前进度：

- [x] shared/db-schema
- [x] shared/ipc-protocol
- [x] core-engine/storage
- [x] core-engine/capture
- [x] ai-sidecar/ocr
- [ ] ai-sidecar/embedding+rag（进行中）
- [ ] core-engine/api-server（已完成基础版）
- [ ] core-engine/executor（已完成基础版）
- [ ] desktop-ui（已完成基础版）
- [ ] ai-sidecar/asr+vlm（待实现）

### 待实现功能

1. **Embedding + RAG**
   - Qdrant 向量库集成
   - 批量向量化任务
   - 语义搜索优化

2. **ASR（语音识别）**
   - Whisper.cpp 集成
   - 音频采集（麦克风）
   - 实时转录

3. **VLM（视觉语言模型）**
   - MiniCPM-V 集成
   - 截图理解
   - 多模态问答

4. **UI 增强**
   - 时间线可视化优化
   - 快捷键支持
   - 系统托盘集成

---

## 附录

### A. 目录结构速查

```
~/.memory-bread/
├── memory-bread.db           # SQLite 数据库
├── memory-bread.db-wal       # WAL 日志
├── memory-bread.db-shm       # 共享内存
├── captures/              # 截图目录
│   ├── 1709510400_app_switch.jpg
│   ├── 1709510450_mouse_click.jpg
│   └── ...
├── models/                # AI 模型缓存
│   ├── paddleocr/
│   ├── bge-m3/
│   └── whisper/
└── logs/                  # 日志文件
    ├── core-engine.log
    └── ai-sidecar.log
```

### B. 端口和路径

| 服务 | 地址 |
|------|------|
| Core Engine API | http://localhost:7070 |
| AI Sidecar IPC (macOS) | /tmp/memory-bread-sidecar.sock |
| AI Sidecar IPC (Windows) | 127.0.0.1:17071 |
| Desktop UI Dev | http://localhost:5173 |
| Qdrant Vector DB | http://localhost:6333 |

### C. 快捷键（计划中）

| 快捷键 | 功能 |
|--------|------|
| Cmd+Shift+W | 唤醒 记忆面包 |
| Cmd+Shift+S | 手动触发采集 |
| Cmd+Shift+F | 打开搜索 |
| Cmd+Shift+T | 打开时间线 |
| Esc | 关闭悬浮窗 |

---

**文档版本**：v1.0
**最后更新**：2026-03-04
**维护者**：记忆面包 开发团队