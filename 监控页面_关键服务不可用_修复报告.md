# 监控页面"关键服务不可用"问题修复报告

## 问题描述

**症状：**
监控页面显示"关键服务不可用"横幅，提示：
- 核心启动检查未通过，仅保留 ping/OCR 能力
- 当前 macOS 10.16，建议升级到 12+ 后再安装 Ollama

**服务状态：**
```json
{
  "status": "down",
  "mode": "basic_ipc",
  "critical_checks_passed": false,
  "full_dispatch_ready": false,
  "background_processor_running": false,
  "embedding_ok": true
}
```

## 根本原因

### 1. 依赖缺失导致检查失败

**问题链：**
```
model_manager.py._is_ollama_running()
  ↓
使用 requests.get() 检查 Ollama API
  ↓
❌ ModuleNotFoundError: No module named 'requests'
  ↓
返回 False（Ollama 未运行）
  ↓
startup_checks.check_ollama_running() → False
  ↓
run_startup_checks() → critical_passed: False
  ↓
main.py bootstrap_full_dispatch() 
  ↓
_write_runtime_status(critical_checks_passed=False)
  ↓
监控页面显示"关键服务不可用"
```

**代码位置：**
```python
# ai-sidecar/model_manager.py:222-229 (修复前)
def _is_ollama_running(self, base_url: str = OLLAMA_API_BASE) -> bool:
    url = f"{base_url.rstrip('/')}/api/tags"
    try:
        import requests  # ❌ requests 未在 requirements.txt 中声明
        resp = requests.get(url, timeout=2)
        return resp.status_code == 200
    except Exception:
        return False  # ❌ 导入失败时静默返回 False
```

### 2. requirements.txt 中未声明 requests

```txt
# ai-sidecar/requirements.txt (修复前)
pydantic>=2.0
flask>=2.0
# ...
# ❌ 没有 requests
```

**实际使用 requests 的地方：**
1. `model_manager.py._is_ollama_running()` - 检查 Ollama 运行状态
2. `knowledge/extractor_v2.py._ollama_chat()` - 流式调用 Ollama API

## 修复方案

### 方案 A：替换为标准库（model_manager.py）

对于简单的 HTTP GET 请求，使用 Python 内置的 `urllib.request`：

```python
# ai-sidecar/model_manager.py:222-230 (修复后)
def _is_ollama_running(self, base_url: str = OLLAMA_API_BASE) -> bool:
    url = f"{base_url.rstrip('/')}/api/tags"
    try:
        import urllib.request
        import socket
        req = urllib.request.Request(url, method='GET')
        with urllib.request.urlopen(req, timeout=2) as resp:
            return resp.status == 200
    except (urllib.error.URLError, urllib.error.HTTPError, socket.timeout, OSError):
        return False
```

**优势：**
- ✅ 不需要额外依赖
- ✅ Python 内置，任何环境都可用
- ✅ 足够满足简单 GET 请求的需求

### 方案 B：显式声明依赖（requirements.txt）

对于需要流式调用等高级功能的场景（extractor_v2.py），必须使用 requests：

```txt
# ai-sidecar/requirements.txt (修复后)
pydantic>=2.0               # 数据验证（ipc-protocol 依赖）
requests>=2.31              # HTTP 客户端（extractor_v2 流式调用 Ollama）  ← 新增
flask>=2.0                  # HTTP 服务框架
# ...
```

## 验证结果

### 修复前
```bash
$ python3 startup_checks.py
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2️⃣  检查 Ollama 服务...
   ❌ Ollama 服务未运行
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
❌ 核心检查未通过，请先完成上述配置
```

### 修复后
```bash
$ python3 startup_checks.py
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2️⃣  检查 Ollama 服务...
   ✅ Ollama 服务运行中
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 所有检查通过，可以启动记忆面包
```

### 运行时状态
```bash
$ curl -s "http://localhost:7070/api/monitor/overview?range_ms=3600000" | jq '.service_health'
{
  "status": "ok",                           ← ✅ 从 "down" 变为 "ok"
  "mode": "full",                           ← ✅ 从 "basic_ipc" 变为 "full"
  "critical_checks_passed": true,           ← ✅ 从 false 变为 true
  "full_dispatch_ready": true,
  "background_processor_running": true,
  "embedding_ok": true,
  "issues": [],                             ← ✅ 无错误
  "updated_at_ms": 1786907532000
}
```

## 修复提交

### Commit 1: 修复 model_manager.py
```
e8c5201 fix: 修复 model_manager.py 中 Ollama 运行检查依赖 requests 模块的问题

- 将 requests.get() 替换为 urllib.request.urlopen()
- 不需要额外依赖，使用 Python 内置模块
- 添加完整的异常处理（URLError, HTTPError, socket.timeout, OSError）
```

### Commit 2: 添加 requests 依赖
```
8577ab4 fix: 将 requests 添加到 requirements.txt

- extractor_v2.py 使用 requests 进行流式 Ollama 调用
- 显式声明 requests>=2.31 依赖
- 添加注释说明用途
```

## 影响范围

### 修复前的问题影响

1. **监控页面误报**
   - 显示"关键服务不可用"
   - 用户看到错误提示，但实际服务正常
   - 降低用户信心

2. **功能降级**
   - Sidecar 运行在 `basic_ipc` 模式
   - 仅保留 ping/OCR 能力
   - 关键功能（时间线提炼、bake、RAG）不可用

3. **依赖风险**
   - requests 作为隐式传递依赖
   - 在某些打包环境可能缺失
   - 导致难以排查的运行时错误

### 修复后的改进

1. **服务状态准确**
   - ✅ 正确识别 Ollama 运行状态
   - ✅ critical_checks_passed: true
   - ✅ 完整功能可用

2. **依赖明确**
   - ✅ model_manager.py 不依赖外部库
   - ✅ requirements.txt 显式声明所有依赖
   - ✅ 打包和部署更可靠

3. **错误处理完善**
   - ✅ 精确的异常类型捕获
   - ✅ 不会将导入错误误判为服务不可用

## 后续建议

### 1. 依赖审计
对所有 Python 代码进行依赖扫描，确保：
- 所有 import 的第三方库都在 requirements.txt 中声明
- 标注依赖用途（如注释）
- 区分必需依赖和可选依赖

### 2. 启动检查增强
```python
# 建议：在启动时验证关键依赖
def check_dependencies():
    required = ['flask', 'pydantic', 'requests', 'ollama']
    missing = []
    for lib in required:
        try:
            __import__(lib)
        except ImportError:
            missing.append(lib)
    if missing:
        raise RuntimeError(f"缺少关键依赖: {', '.join(missing)}")
```

### 3. 监控告警优化
- 区分"依赖缺失"和"服务不可用"
- 提供更具体的错误诊断信息
- 添加自动修复建议（如 pip install）

### 4. CI/CD 验证
- 在 DMG 打包前运行 `startup_checks.py`
- 确保所有依赖已正确打包
- 自动化测试覆盖健康检查流程

## 测试验证

### 本地验证
```bash
# 1. 验证 Ollama 检查
python3 -c "from startup_checks import check_ollama_running; print('OK' if check_ollama_running() else 'FAIL')"
# 预期输出：OK

# 2. 完整启动检查
python3 startup_checks.py
# 预期：所有检查通过

# 3. 验证服务健康状态
curl -s "http://localhost:7070/api/monitor/overview?range_ms=3600000" | jq '.service_health.status'
# 预期输出："ok"
```

### DMG 打包后验证
```bash
# 重新打包
./build-and-install-dmg.sh

# 安装后打开应用，检查监控页面
# 预期：不显示"关键服务不可用"横幅
```

## 总结

**问题根源：**
依赖管理不严格，`model_manager.py` 使用了未声明的 `requests` 库，导致 Ollama 运行状态检查失败。

**修复方案：**
1. 将简单的 HTTP 请求替换为标准库 `urllib.request`（无需外部依赖）
2. 在 `requirements.txt` 中显式声明 `requests` 依赖（供其他需要的模块使用）

**修复效果：**
- ✅ 启动检查正确通过
- ✅ 服务健康状态准确反映实际情况
- ✅ 监控页面不再误报"关键服务不可用"
- ✅ 依赖关系清晰，打包部署更可靠

**提交记录：**
- `e8c5201` - 修复 model_manager.py 的 Ollama 检查
- `8577ab4` - 添加 requests 到 requirements.txt
